export class RowWinInspector {
    constructor(columns) {
        this.columns = columns;
    }

    inspect() {
        for (let i = 5; i >= 0; i--) {
            //This.columns is an object with 4 columns in it.
            // this.columns.token[i];
        }
    }
}
